# fusion-zh Prompts

> 项目：0x2E/fusion 汉化版（fusion-zh）
> 技术栈：React + Vite + Radix UI 前端，Go + SQLite 后端，内置 i18n（默认中文）

---

## 功能迭代

**1. 添加文章稍后阅读功能**
在 fusion-zh 中为文章添加"稍后阅读"标记功能。用户可以将感兴趣的文章加入稍后阅读列表，在侧边栏添加"稍后阅读"入口，支持批量标记和清除。后端需要新增 `read_later` 字段和对应 API。

**2. 支持文章全文抓取**
在 fusion-zh 中添加全文抓取功能。对于只提供摘要的 RSS 源，用户可以点击"获取全文"按钮，后端通过 Go 的 HTTP 客户端抓取原始页面并提取正文内容（使用 readability 算法），替换摘要显示。

**3. 添加订阅源健康状态监控**
在 fusion-zh 的订阅源列表中，显示每个订阅源的健康状态（最后成功抓取时间、连续失败次数）。对于连续失败超过 3 次的订阅源，在侧边栏显示警告图标，并在设置页面提供批量修复建议。

**4. 支持文章内容关键词高亮**
在 fusion-zh 的文章详情页，当用户通过搜索进入文章时，自动高亮显示搜索关键词。支持多关键词高亮（不同颜色），并提供上一处/下一处导航按钮。

**5. 添加 RSS 源推荐功能**
在 fusion-zh 的添加订阅源对话框中，添加"热门推荐"标签页，按分类（科技、新闻、设计等）展示预设的优质 RSS 源列表，用户可以一键订阅，降低新用户的使用门槛。

---

## Bug 修复

**6. 修复中文搜索结果不准确**
在 fusion-zh 中，使用 SQLite 的全文搜索（FTS）对中文内容搜索时，结果不准确，无法按词语边界分词。请集成 jieba 分词或使用 SQLite 的 unicode61 tokenizer，改善中文搜索体验。

**7. 修复文章列表无限滚动在快速滚动时重复加载**
在 fusion-zh 的文章列表中，快速滚动到底部时，无限滚动逻辑会触发多次加载请求，导致文章重复显示。加个加载状态锁，确保同一时间只有一个加载请求在进行。

**8. 修复 OPML 导入时包含特殊字符的订阅源名称被截断**
在 fusion-zh 中导入包含 `&`、`<`、`>` 等 XML 特殊字符的 OPML 文件时，订阅源名称会被截断或显示为 HTML 实体。在解析 OPML 时正确处理 XML 实体转义。

**9. 修复文章已读状态在多标签页间不同步**
在 fusion-zh 中，在一个浏览器标签页将文章标记为已读后，另一个标签页的文章列表不会更新已读状态。请使用 BroadcastChannel API 或 localStorage 事件在多标签页间同步状态变更。

**10. 修复订阅源图标在 HTTPS 混合内容时无法显示**
在 fusion-zh 中，部分订阅源的 favicon 使用 HTTP 协议，在 HTTPS 部署环境下因混合内容策略被浏览器阻止显示。在后端代理 favicon 请求，统一通过 HTTPS 提供图标。

---

## 重构

**11. 将文章列表状态管理迁移到 URL 参数**
fusion-zh 目前使用 Zustand store 管理文章列表的筛选状态（已读/未读/已星标）。把这些状态迁移到 URL 查询参数，使用户可以通过分享 URL 直接访问特定筛选视图，并支持浏览器前进/后退。

**12. 将 Go 后端的数据库操作统一为 Repository 模式**
fusion-zh 的 Go 后端中，部分路由直接执行 SQL 查询，部分通过 service 层。请统一所有数据库操作通过 Repository 接口，便于后续添加缓存层或切换数据库。

---

## 测试

**13. 为 RSS 解析器编写单元测试**
为 fusion-zh 的 Go RSS 解析器编写单元测试，覆盖：标准 RSS 2.0 格式、Atom 格式、包含 CDATA 的内容、中文字符、特殊字符转义、缺失必填字段的处理。

**14. 为文章 API 编写集成测试**
使用 Go 的 `net/http/httptest` 为 fusion-zh 的文章相关 API 编写集成测试，覆盖：获取文章列表（分页/筛选）、标记已读/未读、星标/取消星标、搜索文章。使用内存 SQLite 数据库隔离测试。

**15. 为前端 i18n 翻译完整性编写测试**
为 fusion-zh 编写翻译完整性检查脚本，对比中文翻译文件（zh.ts）与英文基准文件（en.ts），找出缺失的翻译 key，并在 CI 中运行此检查，防止新增功能时遗漏中文翻译。

---

## 代码理解

**16. 解释 fusion 的订阅源抓取调度机制**
在 fusion-zh 中，后端定期抓取所有订阅源的最新文章。解释抓取任务的调度方式（定时器还是队列）、如何控制并发抓取数量避免对目标服务器造成压力、抓取失败时的重试策略，以及如何处理订阅源的 ETag/Last-Modified 缓存头减少不必要的请求。

**17. 解释 fusion 的前后端集成方式**
在 fusion-zh 中，Go 后端通过 `embed.FS` 将 React 前端的构建产物嵌入到单个二进制文件中。解释这种集成方式的工作原理、前端路由如何与后端 API 路由共存、开发模式下如何分离前后端调试。

**18. 解释 SQLite WAL 模式在 fusion 中的作用**
fusion-zh 使用 SQLite 作为数据库，并启用了 WAL（Write-Ahead Logging）模式。解释 WAL 模式相比默认 journal 模式的优势、在 RSS 阅读器场景下（频繁读写）的性能影响，以及如何配置 SQLite 连接池参数。

---

## DevOps

**19. 编写 GitHub Actions 多架构构建流水线**
为 fusion-zh 编写 `.github/workflows/docker-build.yml`，实现推送 main 分支时自动构建多架构（amd64/arm64）Docker 镜像并推送到 Docker Hub。需要分别缓存 pnpm 依赖和 Go 模块缓存以加速构建。

**20. 编写 docker-compose.yml 生产部署配置**
为 fusion-zh 编写 `docker-compose.yml`，包含：fusion 服务（映射 8080 端口）、数据目录挂载（`./data:/data`）、环境变量配置（时区、认证配置）、健康检查、Nginx 反向代理（HTTPS）、自动重启策略。
